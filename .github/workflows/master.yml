name: Node.js CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:

    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v2
    - name: build_push_tag of main
      run: |
             sudo docker build -t niskarsha10/nodejsimage:latest .

